
# --------------------------------------- BUILD ---------------------------------------
# job_spiro_el8-gcc10-impi:build:
#   variables:
#     COMPILER: "gcc@10"
#     MPI_LIB: "impi"
#   extends:
#     - .spiro_el8:before_script:default
#     - .spiro_el8:build:default

job_spiro_el8-gcc12-impi:build:
  variables:
    COMPILER: "gcc@12"
    MPI_LIB: "impi"
  extends:
    - .spiro_el8:before_script:default
    - .spiro_el8:build:default

# job_spiro_el8-gcc10-openmpi:build:
#   variables:
#     COMPILER: "gcc@10"
#     MPI_LIB: "openmpi"
#   extends:
#     - .spiro_el8:before_script:default
#     - .spiro_el8:build:default

job_spiro_el8-gcc12-openmpi:build:
  variables:
    COMPILER: "gcc@12"
    MPI_LIB: "openmpi"
  extends:
    - .spiro_el8:before_script:default
    - .spiro_el8:build:default

# --------------------------------------- TEST ---------------------------------------
# job_spiro_el8-gcc10-impi:test:
#   variables:
#     COMPILER: "gcc@10"
#     MPI_LIB: "impi"
#   extends:
#     - .spiro_el8:before_script:default
#     - .spiro_el8:test:default

job_spiro_el8-gcc12-impi:test:
  variables:
    COMPILER: "gcc@12"
    MPI_LIB: "impi"
  extends:
    - .spiro_el8:before_script:default
    - .spiro_el8:test:default

# job_spiro_el8-gcc10-openmpi:test:
#   variables:
#     COMPILER: "gcc@10"
#     MPI_LIB: "openmpi"
#   extends:
#     - .spiro_el8:before_script:default
#     - .spiro_el8:test:default

job_spiro_el8-gcc12-openmpi:test:
  variables:
    COMPILER: "gcc@12"
    MPI_LIB: "openmpi"
  extends:
    - .spiro_el8:before_script:default
    - .spiro_el8:test:default
